Flyweight é um padrão de projetil de otimização de memoria, ele permite a criação de centenas a milhares de objetos de forma otimizada, 
se esses objetos (que pertecem a uma mesma classe) possuirem valores de atributo iguais, ele utiliza um mesmo espaço de memoria para tratar 
todos esses atributos  
